#!/usr/bin/env bash
set -euo pipefail

# Pressing the shortcut again closes the menu.
if pgrep -x nwg-bar >/dev/null 2>&1; then
    pkill -x nwg-bar
    exit 0
fi

exec nwg-bar \
    -f \
    -p center \
    -a middle \
    -i 76 \
    -t bar.json \
    -s style.css
